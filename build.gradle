
// delegate to consumer
['dynamic', 'static', 'dynamicWorkerApi'].each { id ->
   String taskName = "${id}Greeting"
   tasks.register(taskName) {
      dependsOn(gradle.includedBuild("consumer").task(":$taskName"))
   }
}


